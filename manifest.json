{
  "manifest_version": 3,
  "name": "Tab Sorter",
  "version": "1.1",
  "description": "Sort all tabs in the current window alphabetically (A -> Z) when the extension icon is clicked.",
  "permissions": ["tabs"],
  "action": {
    "default_title": "Sort Tabs"
  },
  "background": {
    "service_worker": "background.js"
  }
}
